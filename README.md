This is an automated scanner tool this tool is strictly made for beginner and intermediate people 
This tools helps you to do many scans on a network just by a click of a button you don't need to remember commands 
You can also perfom a custom script scan that is already present in the tool called NMAP

Why would you choose this tool over NMAP ? 
Answer:- This tool dosen't require you to rember any command and it saves a lot of time because you have to just choose the scan you want to perform and it will perform the scan all by itself and it will show you the result while storing those same results in txt , xml , log form which will help you to make the reports and you can check the result again by opening those files

This tool is only for linux and not for any other OS users because it is completly written in bash langue and the commands it uses only works in linux 
In linux this tool runs in Debian/Ubuntu distibution because it uses apt for installing the dependecies and other distributions don't use these 
